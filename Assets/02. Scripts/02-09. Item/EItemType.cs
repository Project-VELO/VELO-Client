/// <summary>
/// 착용 아이템의 종류를 정의하는 열거형입니다.
/// 의상과 악세사리는 1차 MVP에서 "팀 공통으로 하나만 선택, 능력·외형 반영 없음"이라는 동일한 규칙을 따르므로
/// 데이터 구조를 나누지 않고 이 값으로만 구분합니다(기획서 3-H-6).
///
/// 선택 중인 아이템 ID가 세이브에 저장되므로, 새 종류는 맨 뒤에만 추가합니다.
/// </summary>
public enum EItemType
{
    /// <summary>
    /// 의상입니다. 팀 전체에 하나만 적용됩니다.
    /// </summary>
    COSTUME,

    /// <summary>
    /// 악세사리입니다. 팀 전체에 하나만 적용됩니다.
    /// </summary>
    ACCESSORY
}
