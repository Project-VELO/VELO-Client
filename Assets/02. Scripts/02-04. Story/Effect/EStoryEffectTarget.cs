/// <summary>
/// TINT·FLASH가 어느 덮개를 쓰는지입니다.
///
/// 화면 전체를 고르게 덮는 것과 가장자리만 어둡게 죄는 것은 알파 곡선이 아니라 그림이 다릅니다.
/// 셰이더 없이 내려면 덮개를 두 장 두고 어느 쪽을 쓸지 데이터가 정하는 편이 단순합니다.
///
/// 대사 JSON과의 호환을 위해 새 값은 맨 뒤에만 추가합니다.
/// </summary>
public enum EStoryEffectTarget
{
    /// <summary>
    /// 화면 전체를 고르게 덮는 단색 판입니다. 암전·화이트아웃·색 필터가 씁니다.
    /// </summary>
    OVERLAY,

    /// <summary>
    /// 가장자리만 어두운 비네팅 그림입니다. 시선을 가운데로 모으는 연출이 씁니다.
    /// </summary>
    VIGNETTE
}
